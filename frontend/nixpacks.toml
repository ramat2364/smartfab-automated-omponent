# Nixpacks Configuration for Frontend Railway Deployment
providers = ["node"]

[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["NODE_OPTIONS='--max-old-space-size=1536' npm run build"]

[start]
cmd = "npm start"
